# MVP TODO (Docs/UI First)

## Phase 1: Docs/UI (Ship First)
- Make pip-only the primary path (remove repo clone from primary instructions).
- Add terminal-specific commands:
  - macOS/Linux (bash/zsh)
  - Windows PowerShell
  - WSL
- Combine API docs + Schema into a single section/tab.
- Add plug-and-play input/output examples (no blanks), per preset.
- Simplify setup instructions (one-command setup first, minimal terminal switching).

## Phase 2: Background Run (After Phase 1)
- Add a default "run in background" option for macOS/Linux and Windows.

## Phase 3: SDK (Post-MVP)
- Add a small SDK wrapper for `/invoke` (JS + Python).
